# ============================================================
#  مجلد داخلي — لا يُخدم عبر المتصفّح إطلاقاً
#  Internal directory — never served over HTTP.
#
#  هذا الحارس موضعيّ عن قصد: يعمل حتى لو كان mod_rewrite معطّلاً،
#  ولا يمكن أن يصطدم بأي مسار في التطبيق لأنه لا يُطبَّق إلا حين
#  يربط أباتشي الطلب بهذا المجلد الحقيقي على القرص.
# ============================================================
Require all denied
